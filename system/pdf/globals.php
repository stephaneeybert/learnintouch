<?

define('FPDF_FONTPATH', $gApiPath . 'fpdf/font/');

// The standard line height, specially used by the header and footer
define('PDF_LINE_HEIGHT', 5);

// An inch is that many millimeters
define('PDF_MM_PER_INCH', 25.4);

// The images are rendered in 72 dpi
define('PDF_IMAGE_DOT_PER_INCH', 72);

// The page width in mm for the A4 format
define('PDF_A4_PAGE_WIDTH', 209.90);

// The page height in mm for the A4 format
define('PDF_A4_PAGE_HEIGHT', 297);

?>
